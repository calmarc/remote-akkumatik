Remote Akkumatik Control

Config file: exe_dir/config.txt

Example Config
--
# remote akkumatik config file

serial_port = "/dev/ttyS0"
viewer = /usr/local/bin/qiv
hart_path = /home/calmar/remote-akkumatik/data
tmp_path =  /tmp/remote-akkumatik
--

TODO:

Add config file for:
    picture viewer
    serial port
    tmp data directory (if std tmp not desired)

Matplotlib instead of gnuplot? For windoof it would be
easier possibly.
