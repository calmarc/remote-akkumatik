Remote Akkumatik Control

TODO:

Add config file for:
    picture viewer
    serial port
    tmp data directory (if std tmp not desired)

Matplotlib instead of gnuplot? For windoof it would be
easier possibly.
